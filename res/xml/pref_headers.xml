<preference-headers xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- These settings headers are only used on tablets. -->

	<header
	    android:fragment="${packageName}.${activityClass}$EnablePreferenceFragment"
	    android:title="@string/pref_header_enable" />
    <header
        android:fragment="${packageName}.${activityClass}$GeneralPreferenceFragment"
        android:title="@string/pref_header_general" />
    <header
        android:fragment="${packageName}.${activityClass}$MorsePreferenceFragment"
        android:title="@string/pref_header_morse" />

</preference-headers>